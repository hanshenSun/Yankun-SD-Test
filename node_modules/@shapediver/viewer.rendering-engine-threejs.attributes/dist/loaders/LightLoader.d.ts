import * as THREE from 'three';
import { Box } from '@shapediver/viewer.shared.math';
import { AbstractLight } from '@shapediver/viewer.rendering-engine.light-engine';
import { SDObject } from '../types/SDObject';
import { RenderingEngine } from '../RenderingEngine';
import { ILoader } from '../interfaces/ILoader';
export declare class LightLoader implements ILoader {
    private readonly _renderingEngine;
    private _shadowMapCount;
    private _forceDisabledShadows;
    constructor(_renderingEngine: RenderingEngine);
    init(): void;
    load(light: AbstractLight, parent: SDObject, scene: THREE.Scene, boundingBox: Box): void;
    get shadowMapCount(): number;
    set shadowMapCount(value: number);
    get forceDisabledShadows(): boolean;
    set forceDisabledShadows(value: boolean);
}
//# sourceMappingURL=LightLoader.d.ts.map