import * as THREE from 'three';
import { GeometryData, PrimitiveData, SDTFAttributeVisualizationData } from '@shapediver/viewer.shared.types';
import { Box } from '@shapediver/viewer.shared.math';
import { SDObject } from '../types/SDObject';
import { RenderingEngine } from '../RenderingEngine';
import { ILoader } from '../interfaces/ILoader';
export declare class GeometryLoader implements ILoader {
    private readonly _renderingEngine;
    private _geometryCache;
    private _logger;
    private _counter;
    constructor(_renderingEngine: RenderingEngine);
    emptyGeometryCache(): void;
    removeFromGeometryCache(id: string): void;
    init(): void;
    load(geometry: GeometryData, parent: SDObject, visualizationData: SDTFAttributeVisualizationData): Box;
    loadGeometry(primitive: PrimitiveData): THREE.BufferGeometry;
    private convertToTriangleMode;
}
//# sourceMappingURL=GeometryLoader.d.ts.map