import * as THREE from 'three';
import { vec3 } from 'gl-matrix';
import { IManager } from '../interfaces/IManager';
import { RenderingEngine } from '../RenderingEngine';
export declare class CameraManager implements IManager {
    private readonly _renderingEngine;
    private readonly _orthographicCameraThree;
    private readonly _perspectiveCameraThree;
    constructor(_renderingEngine: RenderingEngine);
    updateCamera(time: number, aspect: number): {
        position: vec3;
        target: vec3;
    };
    adjustCamera(position: vec3, target: vec3, aspect: number): THREE.Camera;
    init(): void;
}
//# sourceMappingURL=CameraManager.d.ts.map