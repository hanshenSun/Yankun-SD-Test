export class TAARenderPass extends SSAARenderPass {
    constructor(scene: any, camera: any, clearColor: any, clearAlpha: any);
    accumulate: boolean;
    accumulateIndex: number | undefined;
    holdRenderTarget: WebGLRenderTarget | undefined;
}
import { SSAARenderPass } from "../postprocessing/SSAARenderPass.js";
import { WebGLRenderTarget } from "three/src/renderers/WebGLRenderTarget";
//# sourceMappingURL=TAARenderPass.d.ts.map