import { vec3 } from 'gl-matrix';
import { ICameraControlsUsage } from '../../interfaces/controls/ICameraControlsUsage';
import { ICamera } from '../../interfaces/camera/ICamera';
export declare class CameraInterpolationManager {
    private readonly _camera;
    private readonly _cameraControls;
    private TweenWrapper;
    private _tween;
    constructor(_camera: ICamera, _cameraControls: ICameraControlsUsage);
    active(): boolean;
    interpolate(path: {
        position: vec3;
        target: vec3;
    }[], options?: {
        easing?: string | Function;
        duration?: number;
        default?: boolean;
        coordinates?: string;
        interpolation?: string | Function;
    }): Promise<boolean>;
    stop(): void;
    private getCameraInterpolation;
    private optionsParser;
}
//# sourceMappingURL=CameraInterpolationManager.d.ts.map