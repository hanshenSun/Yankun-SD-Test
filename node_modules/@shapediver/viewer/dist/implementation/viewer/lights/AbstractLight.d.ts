import { LIGHTTYPE } from '@shapediver/viewer.rendering-engine.light-engine';
import { vec3 } from 'gl-matrix';
import { IViewer } from '../../../interfaces/viewer/IViewer';
import { ILight } from '../../../interfaces/viewer/lights/ILight';
export declare abstract class AbstractLight implements ILight {
    #private;
    constructor(light: ILight, viewer: IViewer);
    get color(): string | number | vec3;
    set color(value: string | number | vec3);
    get id(): string;
    get intensity(): number;
    set intensity(value: number);
    get name(): string | undefined;
    set name(value: string | undefined);
    get order(): number | undefined;
    set order(value: number | undefined);
    get type(): LIGHTTYPE;
}
//# sourceMappingURL=AbstractLight.d.ts.map